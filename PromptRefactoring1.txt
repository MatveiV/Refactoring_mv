Проведи полный аудит файла utils_0.py. В частности, нужен дебаг и рефакторинг кода.

Должны быть вырны следующие требования:
1. Убери небезопасные форматированные запросы.
2. Добавь контекстный менеджер db_connection и инициализацию таблицы init_db, чтобы соединения всегда закрывались и таблица гарантированно существовала.
3. add_user теперь должен:
- не использует мутируемый аргумент по умолчанию;
- работает с копией тегов и всегда добавляет "new", не ломая вызывающий код;
- использует параметризованный запрос INSERT ... VALUES (?, ?);
- возвращает int, как заявлено в документации.
4. Убрал псевдо-"хеш" password[::-1]. Добавлен _hash_password на базе hashlib.pbkdf2_hmac с солью (salt$hash)
5. store_password пишет данные через with open(..., "a"), файл всегда корректно закрывается; формат строки: user_id:salt$hash.
6. Замени глобальный active_users на _active_users с блокировкой _active_users_lock. set_active теперь потокобезопасен, хранит максимум max_active последних ID.
7. Добавь функция get_active_users_snapshot для безопасного чтения списка.
8. Убери искусственную задержка и странная логика удаления первого элемента при >5 пользователей (соответствует коду set_active выше, где удаляются из начала списка излишки по количеству).
9. get_user_by_name теперь:
- возвращает None, если пользователь не найден (а не пустой dict).
- декодирует tags через json.loads.
- гарантирует, что в результате tags — список строк.
10. is_admin больше не ловит все исключения и никогда не возвращает <strong>True</strong> при ошибках.
11. Принимает только dict с ключом 'role'; любые другие значения (включая None, строку, ID и т.п.) считаются не‑админами.
12. long_running_task оставлена синхронной, но упрощён и оптимизирован расчёт.
13. Добавлена run_long_running_task_in_thread, которая запускает задачу в отдельном потоке (асинхронное использование без блокировки основного потока).
14. Старый self_test с заведомо ложными проверками полностью переписан:
проверяет, что add_user возвращает int, пользователь реально создаётся и у него есть тег "new";
проверяет корректность is_admin, работу активных пользователей и запись пароля.
15. Блок if __name__ == "__main__": теперь:
- запускает self_test;
- демонстрирует потокобезопасное добавление нескольких активных пользователей и печать их снапшота.

В целом должен быть проведен качественный рефакторинг и отладка: исправлены ошибки, код обновлён, получен более надежный результат.

