Рассмотри файл api_0.py с Flask-приложением, который также содержит проблемы.

Какие именно?
- нарушены стандарты стиля PEP 8;
- используется глобальное соединение с базой данных для всего приложения;
- присутствуют плохие, неинформативные имена переменных (например, q, kur).

Исправь файл api.py, как ты это сделал ранее.

Должны быть сделаны следующие исправления в файле:
1. Работа с БД
Убрал глобальное соединение conn/cur, добавлен get_db() с использованием g и @teardown_appcontext для автоматического закрытия соединения;
Добавлен init_db() и @before_first_request, таблица создаётся корректно через CREATE TABLE IF NOT EXISTS;
Все SQL-запросы переведены на параметризованные (? + кортеж), SQL-инъекции убраны.

2. Эндпоинт /adduser
Переименован в add_user, приведён к PEP8;
Валидирует JSON {"name": ...}, при пустом/нестроковом имени возвращает 400;
Вставка пользователя безопасно, на выходе JSON {"status": "ok", "id": ..., "name": ...} со статусом 201.

3. Эндпоинт /user/<uid>
Маршрут теперь /user/<int:uid>, параметр уже int;
Использует параметризованный запрос, при отсутствии пользователя возвращает 404 с {"error": "not_found"};
При успехе возвращает 200 и JSON {"id": ..., "name": ...} (код 201 исправлен на 200).

4. Активные пользователи /activate/<uid>
Убран «кривой» поток и глобальный список без блокировок;
Введены _active_users + _active_lock, функции add_active_user и get_active_users (потокобезопасно);
Эндпоинт activate (GET/POST) синхронно добавляет пользователя (с небольшой sleep по желанию) и возвращает {"status": "ok", "active": [...]} без гонок.

5. Медленный эндпоинт /slow: тяжёлая работа вынесена в _slow_task, а сам эндпоинт запускает её в отдельном потоке и сразу отвечает 202 с {"status": "scheduled"}, не блокируя сервер.

6. Обработка ошибок /wrong
Убрано голое except и использование несуществующей переменной;
Теперь ловится ZeroDivisionError и возвращается корректный JSON-ответ {"msg": "error", "detail": "division by zero"} со статусом 500.

7. Общее:
код приведён к PEP8: пробелы, имена функций/переменных, аннотации типов;
запуск приложения переведён в блок if __name__ == "__main__":, включён debug=True для разработки.